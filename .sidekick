
# This is an example sidekick file that i use for testing
# by doing. Proper tests are welcome.

watch '**/*.rb' do |paths|
  log 'files updated'
end

every(20) do
  notify sh('fortune')
end

# vim:ft=ruby
